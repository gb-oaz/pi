{
  "name": "frontend",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev:start": "concurrently \"wsrun --parallel dev:build\" \"wsrun --parallel dev:start\"",
    "prod:start": "concurrently \"wsrun --parallel prod:build\" \"wsrun --parallel prod:start\""
  },
  "workspaces": [
    "*"
  ],
  "dependencies": {
    "concurrently": "^8.2.2",
    "wsrun": "^5.2.3"
  }
}
